# Local dev (Vite proxies /api to the backend)
VITE_API_BASE_URL=http://localhost:8000/api

# Production (Vercel) — backend host only is fine (/api is added automatically)
# VITE_API_BASE_URL=https://ecomm-backend-zgo1.onrender.com
